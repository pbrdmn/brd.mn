---
layout: default.njk
---
<section>
  <header>
    <h1>
      {% if display_title %}
        {{ display_title }}
      {% else %}
        {{ title }}
      {% endif %}
    </h1>
  </header>

  {{ content | safe }}
</section>